import { cn } from '@/utils/cn';

const BottomNavBar = () => {
    return (
        <nav
            className={cn('fixed bottom-0 w-[600px] h-[68px] bg-gray-900 border border-top border-gray-700 box-border')}
        >
            <p className="border">gd</p>
        </nav>
    );
};

export default BottomNavBar;
