import { SearchHeader } from '@/components/search';
import React from 'react';

const Search = () => {
    return (
        <div>
            <SearchHeader />
        </div>
    );
};

export default Search;
