import { NavBottom } from '@/components/layout';
import React from 'react';

const bookmark = () => {
    return (
        <>
            <NavBottom />
        </>
    );
};

export default bookmark;
